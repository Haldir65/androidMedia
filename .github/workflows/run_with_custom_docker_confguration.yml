name: Build with self home made docker image

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  runWithDocker:
    runs-on: ubuntu-latest
    if: "contains(github.event.head_commit.message, '[Docker]')"
    steps:
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "This job is running on a ${{ runner.os }} server hosted by GitHub!"
      - name: Checkout the code
        uses: actions/checkout@v4
      - run: echo "The ${{ github.repository }} repository has been cloned."
      - run: echo "Setting up Docker"
      - name: set up Docker BUildx
        uses: docker/setup-buildx-action@v3

      - run: echo "start building custom Docker image... "
      - name: now build Docker image
        run: docker build --build-arg VERSION=2.294.0-ubuntu-focal --build-arg JAVA_VERSION=17 -f Dockerfile --tag=image1 .
      
      - name: Look up images
        run: docker image ls 

      - run: echo "done building custom Docker image... "
      - name: Run a container from the image in detached mode.
        run: docker run --name=container1 --rm --detach image1

      - name: executing commands in container ,clone required dependencies , glm and jpeg-turbo , etc...
        working-directory: DroidMedia
        run: |
          cat <<EOF | docker exec container1 ls -al
          chmod +x ./.buildscripts/ensure_third_party_deps.sh
          ls -al
          bash ./.buildscripts/ensure_third_party_deps.sh
          ls -al
          echo "begin Building Debug APK."
          ./gradlew assembleDebug
          echo "end Building Debug APK."
          EOF

      - run: echo "Build status report=${{ job.status }}."


   
